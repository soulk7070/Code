// Ambil satu item dari Split In Batches dan siapkan payload untuk API + info baris
const row = $json;
return [
  {
    json: {
      // data buat API Veo2 (samain dengan kebutuhan endpoint kamu)
      apiPayload: {
        prompt: row.prompt,
        ratio: row.ratio,       // contoh: "landscape"
        duration: row.duration  // contoh: "8s" atau angka detik kalau API minta number
      },
      // simpan info baris supaya mudah update balik nanti
      rowIndex: row.rowIndex ?? row._sheet?.rowIndex ?? null
    }
  }
];
